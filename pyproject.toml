[project]
name = "tiktok-trend-detector"
version = "0.1.0"
description = "Real-time TikTok trend detection system"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    # Shared dependencies
    "python-dotenv>=1.2.1",
    # Producer dependencies
    "apify-client>=2.2.1",
    "kafka-python>=2.2.15",
    # Consumer dependencies (will add later)
    # "pyspark>=3.5.0",
    # "openai>=1.0.0",
    # "pymongo>=4.0.0",
    # Dashboard dependencies (will add later)
    # "streamlit>=1.30.0",
    # "pymongo>=4.0.0",
    # "plotly>=5.0.0",
]
